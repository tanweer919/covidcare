import Layout from "../src/components/Layout";

const Care = () => {
  const items = new Array(40).fill(null);

  return <Layout selectedKey={1}>
    <h1>Care</h1>
  </Layout>;
};

export default Care;
